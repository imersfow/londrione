# LondriOne Stage 2A.1 - White Label Branding / Favicon

Adds:
- App name
- App tagline
- Logo URL + preview
- Favicon URL + preview
- Dynamic browser title / favicon through safe public branding RPC
- Sidebar brand follows owner branding

Run `LONDRIONE_STEP_9_WHITE_LABEL_BRANDING.sql` in Supabase first, then upload `app`, `components`, and `lib` to GitHub.
