import { getAppContext } from "@/lib/context";
export default async function SettingsPage(){const{membership}=await getAppContext();const tenant=membership.tenants as any;return <div className="space-y-6"><div><h1 className="page-title">Pengaturan</h1><p className="muted mt-1">Brand, profil bisnis, user dan integrasi akan dikembangkan di sini.</p></div><div className="card max-w-2xl p-6"><div className="text-sm text-slate-500">Bisnis aktif</div><div className="mt-1 text-2xl font-black">{tenant?.name}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-violet-50 p-4"><div className="text-xs text-violet-500">Slug</div><div className="font-semibold">{tenant?.slug}</div></div><div className="rounded-xl bg-sky-50 p-4"><div className="text-xs text-sky-500">Role Anda</div><div className="font-semibold capitalize">{membership.role as string}</div></div></div></div></div>}
